version: '2'

syncs:
  volume-sync:
    sync_strategy: 'native_osx'
    sync_host_ip: '127.0.0.1'
    sync_excludes: ['.gitignore', '.idea/*','.git/*', '*.coffee', '*.scss', '*.sass','*.log']
    src: './'